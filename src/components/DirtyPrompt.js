import React from 'react'
// import {interpolate} from '../util'
// import {useTranslation} from 'react-i18next'
// import {usePrompt} from '../hook/useBlocker'
// // import {Prompt} from './Prompt'
//
// // when={isDirty}
// export const DirtyPrompt = (/*props*/{when, ...props}) => {
//   // const {when} = props
//   const {t} = useTranslation()
//   const message = interpolate(t('routeChangeDirty'), {location})
//   usePrompt(message, when );
//   return (
//       <div>_._</div>
//   )
//   // window.confirm(message)
//   // return <div
//   //     // message={location => interpolate(t('routeChangeDirty'), {location})}
//   //     {...props}
//   // >{message} {when?'true':'false'}</div>
// }

// todo fix
export const DirtyPrompt = ()=>(<></>)
